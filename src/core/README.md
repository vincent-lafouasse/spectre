# core module

elements of code that depend on nothing but standard C and the `definitions.h` header

`definitions.h` depends only on standard C

the main difference between `./modules` and `./src/core` is that core modules are not bundled into libraries
